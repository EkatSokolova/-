# Импортируем необходимые модули
import pygame
import random
from functions import draw_score

# Инициализация библиотеки Pygame
pygame.init()

# Настройка игрового окна
ширина_окна = 800
высота_окна = 600
окно = pygame.display.set_mode((ширина_окна, высота_окна))
pygame.display.set_caption("Школьный проект")

# Определение цветов с помощью RGB значений
белый = (255, 255, 255)
красный = (255, 0, 0)
зеленый = (0, 255, 0)
синий = (0, 0, 255)

# Создание объекта игрока
class Игрок(pygame.sprite.Sprite):


# Остальной код

# В основном цикле игры
while running:
    # Остальной код

    # Отображение счета игрока
    draw_score(score)

    # Остальной код
